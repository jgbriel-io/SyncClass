/**
 * Dialog de exibição de senha gerada para aluno.
 * Reutiliza o mesmo padrão do PasswordDisplayDialog (users).
 */
export { PasswordDisplayDialog as StudentPasswordDialog } from "@/components/users/PasswordDisplayDialog";
