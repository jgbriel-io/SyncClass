/**
 * Reutiliza PasswordDisplayDialog — mesmo padrão do TeacherPasswordDialog.
 * Exibe senha gerada ao criar professor ou após redefinição.
 */
export { PasswordDisplayDialog as TeacherPasswordDialog } from "@/components/users/PasswordDisplayDialog";
